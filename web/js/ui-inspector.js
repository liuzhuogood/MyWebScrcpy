(function () {
  'use strict';
  const canvas = document.getElementById('screen');
  const overlay = document.getElementById('ui-inspector-overlay');
  const serial = new URLSearchParams(location.search).get('serial');
  const button = document.getElementById('btn-inspector');
  const playerMain = document.querySelector('.player-main');
  if (!canvas || !overlay || !button) return;

  let panel, resizer, tabColor, tabXML, readout, normalizedReadout, tree, props, xpath, xpathResult, message, showAllButton;
  let xmlDoc = null, xmlBoundsSize = null, xmlDisplaySize = null, selected = null, xpathNodes = [], locatorAttributes = new Set(), showAllNodes = false, lastPoint = null, crosshairPinned = false, raf = 0, magnifier;
  const nodeRows = new WeakMap();

  function el(tag, text, cls) {
    const node = document.createElement(tag);
    if (text != null) node.textContent = text;
    if (cls) node.className = cls;
    return node;
  }
  function build() {
    panel = el('section', null, 'ui-inspector-panel'); panel.hidden = true; panel.setAttribute('role', 'dialog');
    const head = el('div', null, 'ui-inspector-head'); head.append(el('strong', '检查')); const close = el('button', '关闭'); close.onclick = closePanel; head.append(close); panel.append(head);
    const tabs = el('div', null, 'ui-inspector-tabs'); const colorBtn = el('button', '取色坐标'); const xmlBtn = el('button', 'XML 树'); colorBtn.classList.add('active'); tabs.append(colorBtn, xmlBtn); panel.append(tabs);
    tabColor = el('div', null, 'ui-inspector-tab active'); readout = el('div', null, 'ui-inspector-readout'); normalizedReadout = el('span', '—'); readout.append(el('span', '坐标'), el('span', '移动鼠标到画面上'), el('span', '归一化'), normalizedReadout, el('span', '颜色'), el('span', '—')); tabColor.append(readout); magnifier = document.createElement('canvas'); magnifier.width = magnifier.height = 156; magnifier.className = 'ui-inspector-magnifier'; tabColor.append(magnifier); panel.append(tabColor);
    tabXML = el('div', null, 'ui-inspector-tab'); const refresh = el('button', '刷新 XML'); refresh.onclick = fetchXML; xpath = document.createElement('input'); xpath.className = 'ui-inspector-xpath'; xpath.placeholder = "例如 //*[@resource-id='app:id/login']"; xpath.onkeydown = event => { if (event.key === 'Enter') testXPath(); }; const test = el('button', '测试 XPath'); test.onclick = testXPath; showAllButton = el('button', '显示全部'); showAllButton.onclick = toggleShowAll; const clear = el('button', '清除'); clear.onclick = clearXPath; const controls = el('div', null, 'ui-inspector-controls'); controls.append(refresh, xpath, test, showAllButton, clear); xpathResult = el('div', '尚未测试', 'ui-inspector-message'); message = el('div', '', 'ui-inspector-message'); tree = el('ul', null, 'ui-inspector-tree'); props = el('div', null, 'ui-inspector-props'); tabXML.append(controls, xpathResult, message, tree, props); panel.append(tabXML);
    resizer = el('div', null, 'ui-inspector-resizer'); resizer.setAttribute('aria-hidden', 'true'); playerMain.append(resizer, panel); setupResizer();
    colorBtn.onclick = () => switchTab(colorBtn, xmlBtn, tabColor, tabXML); xmlBtn.onclick = () => { switchTab(xmlBtn, colorBtn, tabXML, tabColor); if (!xmlDoc) fetchXML(); };
    button.onclick = () => { panel.hidden = !panel.hidden; const opened = !panel.hidden; playerMain.classList.toggle('inspector-open', opened); if (opened) { playerMain.classList.remove('inspector-resized'); playerMain.style.removeProperty('--inspector-width'); setMoreMenuClosed(); syncOverlay(); drawBoxes(); } else clearOverlay(); button.setAttribute('aria-expanded', String(opened)); window.dispatchEvent(new Event('resize')); };
  }
  function switchTab(active, other, pane, otherPane) { active.classList.add('active'); other.classList.remove('active'); pane.classList.add('active'); otherPane.classList.remove('active'); drawBoxes(); }
  function setMoreMenuClosed() { const menu = document.getElementById('more-menu'); if (menu) menu.hidden = true; }
  function clearOverlay() { overlay.getContext('2d').clearRect(0, 0, overlay.width, overlay.height); }
  function closePanel() { panel.hidden = true; playerMain.classList.remove('inspector-open'); button.setAttribute('aria-expanded', 'false'); if (selected) selected.row.classList.remove('selected'); selected = null; locatorAttributes.clear(); xpathNodes = []; showAllNodes = false; clearOverlay(); window.dispatchEvent(new Event('resize')); }
  function setupResizer() { resizer.addEventListener('pointerdown', event => { if (innerWidth <= 820) return; const startX = event.clientX; const startWidth = panel.getBoundingClientRect().width; playerMain.classList.add('inspector-resized'); playerMain.style.setProperty('--inspector-width', `${startWidth}px`); resizer.classList.add('dragging'); resizer.setPointerCapture(event.pointerId); const move = moveEvent => { const maxWidth = Math.max(420, playerMain.clientWidth - 452); const width = Math.max(420, Math.min(maxWidth, startWidth + startX - moveEvent.clientX)); playerMain.style.setProperty('--inspector-width', `${width}px`); window.dispatchEvent(new Event('resize')); }; const stop = () => { resizer.classList.remove('dragging'); resizer.removeEventListener('pointermove', move); resizer.removeEventListener('pointerup', stop); resizer.removeEventListener('pointercancel', stop); }; resizer.addEventListener('pointermove', move); resizer.addEventListener('pointerup', stop); resizer.addEventListener('pointercancel', stop); }); }
  function syncOverlay() { const canvasRect = canvas.getBoundingClientRect(); const parent = overlay.offsetParent || canvas.parentElement; const parentRect = parent.getBoundingClientRect(); overlay.width = canvas.width; overlay.height = canvas.height; overlay.style.left = `${canvasRect.left - parentRect.left}px`; overlay.style.top = `${canvasRect.top - parentRect.top}px`; overlay.style.width = `${canvasRect.width}px`; overlay.style.height = `${canvasRect.height}px`; }
  function sourcePoint(event) { const rect = canvas.getBoundingClientRect(); return { x: Math.max(0, Math.min(canvas.width - 1, Math.floor((event.clientX - rect.left) * canvas.width / rect.width))), y: Math.max(0, Math.min(canvas.height - 1, Math.floor((event.clientY - rect.top) * canvas.height / rect.height))) }; }
  function updateColor(event) { if (!canvas.width || !canvas.height || crosshairPinned) return; lastPoint = sourcePoint(event); if (!raf) raf = requestAnimationFrame(drawColor); }
  function drawColor() { raf = 0; if (!lastPoint) return; const pixel = canvas.getContext('2d').getImageData(lastPoint.x, lastPoint.y, 1, 1).data; const hex = '#' + [pixel[0], pixel[1], pixel[2]].map(value => value.toString(16).padStart(2, '0')).join('').toUpperCase(); const normalizedX = canvas.width > 1 ? lastPoint.x / (canvas.width - 1) : 0; const normalizedY = canvas.height > 1 ? lastPoint.y / (canvas.height - 1) : 0; readout.children[1].textContent = `${lastPoint.x} × ${lastPoint.y}`; normalizedReadout.textContent = `${normalizedX.toFixed(4)} × ${normalizedY.toFixed(4)}`; readout.children[5].textContent = `${hex}  rgb(${pixel[0]}, ${pixel[1]}, ${pixel[2]})`; const context = magnifier.getContext('2d'); context.imageSmoothingEnabled = false; const size = 13, sx = Math.max(0, Math.min(canvas.width - size, lastPoint.x - 6)), sy = Math.max(0, Math.min(canvas.height - size, lastPoint.y - 6)); context.clearRect(0, 0, 156, 156); context.drawImage(canvas, sx, sy, size, size, 0, 0, 156, 156); context.strokeStyle = '#fff'; context.lineWidth = 2; context.strokeRect((lastPoint.x - sx) * 12, (lastPoint.y - sy) * 12, 12, 12); drawBoxes(); }
  canvas.addEventListener('mousemove', updateColor); canvas.addEventListener('mouseleave', () => { if (!crosshairPinned) { lastPoint = null; drawBoxes(); } });
  // Capture before player.html's mousedown handler so an inspector hit is read-only.
  canvas.addEventListener('mousedown', handleInspectorMouseDown, true);

  function fetchXML() { message.className = 'ui-inspector-message'; message.textContent = '正在获取 XML…'; if (selected) selected.row.classList.remove('selected'); selected = null; locatorAttributes.clear(); xpathNodes = []; showAllNodes = false; showAllButton.textContent = '显示全部'; xpath.value = ''; xpathResult.className = 'ui-inspector-message'; xpathResult.textContent = '尚未测试'; tree.replaceChildren(); props.textContent = ''; clearOverlay(); fetch(`/api/ui/xml?serial=${encodeURIComponent(serial || '')}`).then(response => response.ok ? response.json() : response.json().then(data => Promise.reject(new Error(data.message || '获取失败')))).then(data => { const parsed = new DOMParser().parseFromString(data.xml, 'application/xml'); if (parsed.querySelector('parsererror')) throw new Error('XML 格式无效'); xmlDoc = parsed; xmlBoundsSize = measureXMLBounds(parsed); xmlDisplaySize = validDisplaySize(data.display_size) ? data.display_size : null; renderTree(); props.textContent = `快照时间：${data.captured_at}`; message.textContent = ''; syncOverlay(); }).catch(error => { xmlDoc = null; xmlBoundsSize = null; xmlDisplaySize = null; tree.replaceChildren(); props.textContent = ''; message.className = 'ui-inspector-message ui-inspector-error'; message.textContent = error.message; }); }
  function renderTree() { tree.replaceChildren(); const root = xmlDoc && xmlDoc.documentElement; if (!root) { message.textContent = '设备未提供可检查节点'; return; } tree.append(renderNode(root)); }
  function renderNode(node) { const item = el('li'); const children = Array.from(node.children); const row = el('div', null, 'ui-inspector-node'); const toggle = el('button', children.length ? '▾' : '', 'ui-inspector-toggle'); toggle.type = 'button'; toggle.setAttribute('aria-expanded', 'true'); row.append(toggle, el('span', node.tagName, 'ui-inspector-tree-tag')); const summary = [node.tagName]; ['class', 'resource-id', 'text', 'bounds'].forEach(name => { const value = node.getAttribute(name); if (!value) return; const attr = el('span', null, 'ui-inspector-tree-attr'); attr.append(el('span', `${name}=`, 'ui-inspector-tree-attr-name'), el('span', JSON.stringify(value), 'ui-inspector-tree-attr-value')); row.append(attr); summary.push(`${name}=${value}`); }); row.title = summary.join(' '); nodeRows.set(node, row); row.onclick = () => selectNode(node, row); item.append(row); if (children.length) { const childList = el('ul'); children.forEach(child => childList.append(renderNode(child))); toggle.onclick = event => { event.stopPropagation(); childList.hidden = !childList.hidden; toggle.textContent = childList.hidden ? '▸' : '▾'; toggle.setAttribute('aria-expanded', String(!childList.hidden)); }; item.append(childList); } return item; }
  function rawBounds(node) { const match = /^\[(-?\d+),(-?\d+)\]\[(-?\d+),(-?\d+)\]$/.exec(node.getAttribute('bounds') || ''); return match ? { left:+match[1], top:+match[2], right:+match[3], bottom:+match[4] } : null; }
  function measureXMLBounds(doc) { let right = 0, bottom = 0; doc.querySelectorAll('[bounds]').forEach(node => { const value = rawBounds(node); if (value) { right = Math.max(right, value.right); bottom = Math.max(bottom, value.bottom); } }); return right > 0 && bottom > 0 ? { width:right, height:bottom } : null; }
  function validDisplaySize(value) { return value && Number.isFinite(value.width) && Number.isFinite(value.height) && value.width > 0 && value.height > 0; }
  function bounds(node) { const value = rawBounds(node); if (!value) return null; const basis = xmlDisplaySize || xmlBoundsSize; const scaleX = basis && canvas.width ? canvas.width / basis.width : 1; const scaleY = basis && canvas.height ? canvas.height / basis.height : 1; return { x:value.left * scaleX, y:value.top * scaleY, w:(value.right - value.left) * scaleX, h:(value.bottom - value.top) * scaleY, node }; }
  function resetLocatorAttributes(node) { locatorAttributes = new Set(); if (node.getAttribute('resource-id')) locatorAttributes.add('resource-id'); else if (node.getAttribute('text')) locatorAttributes.add('text'); }
  function locatorCheckbox(attribute) { const checkbox = document.createElement('input'); checkbox.type = 'checkbox'; checkbox.className = 'ui-inspector-locator-checkbox'; checkbox.checked = locatorAttributes.has(attribute.name); checkbox.disabled = !attribute.value; checkbox.setAttribute('aria-label', `${attribute.name} 参与定位`); checkbox.title = attribute.value ? `${attribute.name} 参与定位` : `${attribute.name} 为空，不能参与定位`; checkbox.onchange = () => { if (checkbox.checked) locatorAttributes.add(attribute.name); else locatorAttributes.delete(attribute.name); renderProps(selected.node); testXPath(); }; return checkbox; }
  function renderProps(node) { props.replaceChildren(); Array.from(node.attributes).forEach(attribute => { const line = el('div', null, 'ui-inspector-prop-line'); const checkbox = locatorCheckbox(attribute); const value = el('span', JSON.stringify(attribute.value), 'ui-inspector-prop-value'); if (/^(true|false)$/.test(attribute.value)) value.classList.add('boolean'); line.append(checkbox, el('span', attribute.name, 'ui-inspector-prop-name'), el('span', '=', 'ui-inspector-prop-equals'), value); props.append(line); }); xpath.value = makeXPath(node, locatorAttributes); }
  function selectNode(node, row) { if (selected && selected.node === node) { selected.row.classList.remove('selected'); selected = null; locatorAttributes.clear(); props.textContent = ''; drawBoxes(); return; } if (selected) selected.row.classList.remove('selected'); selected = { node, row }; resetLocatorAttributes(node); row.classList.add('selected'); renderProps(node); testXPath(); }
  function findBoxAtPoint(event) {
    if (panel.hidden || !tabXML.classList.contains('active') || !xmlDoc) return null;
    const point = sourcePoint(event);
    return visibleBoxes().concat(selected ? [bounds(selected.node)].filter(Boolean) : [])
      .filter(box => point.x >= box.x && point.x <= box.x + box.w && point.y >= box.y && point.y <= box.y + box.h)
      .sort((a, b) => a.w * a.h - b.w * b.h)[0] || null;
  }
  function handleInspectorMouseDown(event) {
    if (event.button !== 0) return;
    if (!panel.hidden && tabColor.classList.contains('active')) {
      event.preventDefault();
      event.stopImmediatePropagation();
      crosshairPinned = !crosshairPinned;
      lastPoint = sourcePoint(event);
      if (!raf) raf = requestAnimationFrame(drawColor);
      return;
    }
    const box = findBoxAtPoint(event);
    if (!box) return;
    const row = nodeRows.get(box.node);
    if (!row) return;
    event.preventDefault();
    event.stopImmediatePropagation();
    selectNode(box.node, row);
  }
  function xpathLiteral(value) { if (!value.includes("'")) return `'${value}'`; if (!value.includes('"')) return `"${value}"`; return `concat(${value.split("'").map(part => `'${part}'`).join(', "\'", ')})`; }
  function makeXPath(node, attributes = new Set()) { const predicates = Array.from(attributes).filter(name => node.getAttribute(name)).map(name => `@${name}=${xpathLiteral(node.getAttribute(name))}`); if (predicates.length) return `//*[${predicates.join(' and ')}]`; const parts = []; for (let current = node; current && current.nodeType === 1; current = current.parentElement) { let index = 1; for (let sibling = current.previousElementSibling; sibling; sibling = sibling.previousElementSibling) if (sibling.tagName === current.tagName) index++; parts.unshift(`${current.tagName.toLowerCase()}[${index}]`); } return '/' + parts.join('/'); }
  function visibleBoxes() { if (!xmlDoc) return []; const nodes = showAllNodes ? Array.from(xmlDoc.querySelectorAll('[bounds]')) : xpathNodes; return nodes.map(bounds).filter(Boolean); }
  function drawBoxes() { if (!panel || panel.hidden) return; syncOverlay(); const context = overlay.getContext('2d'); context.clearRect(0, 0, overlay.width, overlay.height); if (tabColor.classList.contains('active') && lastPoint) { context.save(); context.strokeStyle = '#ffd54a'; context.lineWidth = Math.max(1, canvas.width / 900); context.setLineDash([8, 6]); context.beginPath(); context.moveTo(lastPoint.x, 0); context.lineTo(lastPoint.x, canvas.height); context.moveTo(0, lastPoint.y); context.lineTo(canvas.width, lastPoint.y); context.stroke(); context.setLineDash([]); context.beginPath(); context.arc(lastPoint.x, lastPoint.y, Math.max(5, canvas.width / 150), 0, Math.PI * 2); context.stroke(); context.restore(); return; } context.strokeStyle = '#00e676'; context.lineWidth = Math.max(2, canvas.width / 500); visibleBoxes().forEach(box => context.strokeRect(box.x, box.y, box.w, box.h)); if (selected) { const box = bounds(selected.node); if (box) { context.lineWidth = Math.max(3, canvas.width / 350); context.strokeRect(box.x, box.y, box.w, box.h); } } }
  function toggleShowAll() { if (!xmlDoc) return; showAllNodes = !showAllNodes; showAllButton.textContent = showAllNodes ? '隐藏全部' : '显示全部'; xpathResult.className = 'ui-inspector-message'; xpathResult.textContent = showAllNodes ? `已显示 ${visibleBoxes().length} 个元素` : '尚未测试'; drawBoxes(); }
  function clearXPath() { xpath.value = ''; xpathNodes = []; showAllNodes = false; showAllButton.textContent = '显示全部'; xpathResult.className = 'ui-inspector-message'; xpathResult.textContent = '尚未测试'; drawBoxes(); }
  function testXPath() { if (!xmlDoc || !xpath.value.trim()) { clearXPath(); return; } try { const result = xmlDoc.evaluate(xpath.value, xmlDoc, null, XPathResult.ORDERED_NODE_SNAPSHOT_TYPE, null); xpathNodes = []; for (let index = 0; index < result.snapshotLength; index++) { if (bounds(result.snapshotItem(index))) xpathNodes.push(result.snapshotItem(index)); } showAllNodes = false; showAllButton.textContent = '显示全部'; xpathResult.className = 'ui-inspector-message'; xpathResult.textContent = `${result.snapshotLength} 个匹配元素`; drawBoxes(); } catch (_) { xpathNodes = []; xpathResult.className = 'ui-inspector-message ui-inspector-error'; xpathResult.textContent = 'XPath 无效'; drawBoxes(); } }
  build(); window.addEventListener('resize', drawBoxes); document.addEventListener('fullscreenchange', () => setTimeout(drawBoxes, 100));
}());
