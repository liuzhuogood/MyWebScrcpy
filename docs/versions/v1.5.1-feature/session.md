# v1.5.1-feature 会话记录

## 2026-09-09

**用户**：要求创建 1.5.1 版本：首页右上角增加 GitHub 链接，隐藏“脚本管理”入口；将“原始尺寸”改为可在当前播放器页调节的“显示尺寸”，通过带原始尺寸吸附点的滑道调整；并排查和修复开始录制时报 503。

**代理**：在 10.0.0.6 直接调用 `POST /api/recordings` 复现 503，服务返回 `recording directory unavailable: mkdir recordings: permission denied`。systemd 以普通用户启动且没有工作目录，原相对路径被解析为 `/recordings`。确认与本次 503 无关的 ADB 设备和投屏会话均可用。

**代理**：将录制默认目录改为当前用户缓存目录中的受控路径，保留 `RECORDINGS_DIR` 覆盖；录制启动失败写入服务日志。播放器显示尺寸改为当前页弹出层，25%～175% 滑道在 100% 原始尺寸附近吸附，并可恢复适应窗口。

**代理**：已部署到 10.0.0.6。直接调用真实录制接口返回 202，停止后状态为 `completed`；生成的 MP4 位于 `/home/liuzhuo/.cache/mywebscrcpy/recordings/`，经 ffprobe 确认为 448×1024 H.264、时长约 3 秒。浏览器确认首页仅显示 GitHub 和大屏模式入口，播放器滑道的 102% 自动吸附到 100%，150% 显示为 672×1536，恢复按钮回到适应窗口。
