# v1.6.0-feature 实施任务

> 本文件是设计拆解，不代表本轮开始实施。

## A. 接口事实来源

- ⬜ 从 `main.go` 与各 `internal/*` 路由注册处提取当前全部 HTTP/WebSocket 接口。
- ⬜ 为每个接口核对方法、路径参数、查询参数、请求体、响应、状态码、副作用与前提条件。
- ⬜ 新建并维护 OpenAPI 3.1 清单；为无法完整表达的 WebSocket 帧协议补充结构化扩展说明。
- ⬜ 增加路由覆盖校验，确保注册路由与清单一致，并为暂不公开的路由建立显式排除理由。

## B. API 文档页面

- ⬜ 新增 `/api/openapi.json` 只读端点与 `/api-docs.html` 页面。
- ⬜ 在页面首个内容区放入 `my-scrcpy-use` 提示词，并实现可访问的复制交互。
- ⬜ 按设备与控制、实时投屏、录像、ADB/按键、文件、脚本、UI 检查、Vision 渲染接口目录和详情。
- ⬜ 为参数、响应、错误和副作用增加易读标识；示例仅使用安全占位数据。
- ⬜ 在首页加入 API 文档入口，并适配桌面和窄屏阅读。

## C. 技能提示词与质量

- ⬜ 校验提示词明确 `my-scrcpy-use` 命令名、清单来源、覆盖范围、只读优先和副作用确认规则。
- ⬜ 确认提示词不会要求 AI 保存设备隐私内容、令牌、证书或用户文件正文。
- ⬜ 编写 API 清单解析、路由覆盖、页面首段顺序和复制行为测试。
- ⬜ 手工检查 HTTP、HTTPS 与离线环境下的页面加载、锚点跳转和示例可读性。

## D. 发布准备

- ⬜ 更新 README 的 API 文档入口和使用说明。
- ⬜ 回归现有设备管理、投屏、录像、文件、脚本、UI XML 和 Vision 接口，不改变既有响应语义。
- ⬜ 完成 Go 测试、静态检查、前端页面检查和 `git diff --check`；实施完成后再决定正式版本、提交、部署和发布。
