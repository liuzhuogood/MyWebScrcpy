# v1.6.0-feature 版本摘要

1. 新增由受版本控制 OpenAPI 清单驱动的 API 文档页面与 `/api/openapi.json` 只读端点。
2. 页面首段可复制 `my-scrcpy-use` 技能提示词，并自动使用当前浏览器服务地址。
3. 文档页面显示设备、投屏、录像、ADB、文件、UI XML 与 Vision 接口；脚本接口保留在清单但暂不展示。
4. 新增清单和页面地址替换规则测试，既有业务 API 行为未改变。
