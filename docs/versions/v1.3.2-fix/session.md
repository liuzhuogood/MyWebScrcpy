# v1.3.2-fix 会话记录

> 本版本是 v1.3.1 的补丁版本，包含 README 双语对齐和播放器断流体验修复。

## 用户要求

- 优化 README 顶部项目介绍，使其更符合正式开源项目文案。
- 对齐中文版与英文版 README 的内容结构。
- 提交、推送并创建新的版本 tag。

## 本轮改动

- 将 README 顶部改为项目定位、核心能力和 ADB 使用前提的完整说明。
- 为英文 README 补齐大屏监控、文件管理截图，以及 Docker、命令行参数和 HTTPS 配置章节。
- 同步中英文 README 的特性、环境变量、项目结构和技术栈说明。
- 更新正式版本号为 `v1.3.2`。

## 验证结果

- `git diff --check` 通过。
- README 文案检查通过，仅保留原有弯引号风格提示。
- 中英文 README 章节和截图引用已逐项对应。
- `go test ./...`、`go vet ./...`、前端语法检查已通过。
